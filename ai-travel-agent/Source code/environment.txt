pip install fastapi[all]
pip install openai
pip install python-dotenv
pip install pydantic==1.*
pip install langchain
pip install bs4
pip install tiktoken


Operation Manual:
After install all pip packages, type "streamlit run <pythong file name>.py" in the terminal to run the web
